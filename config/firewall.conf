# Personal Firewall Default Configuration
# Customize this file according to your needs

[general]
chain=INPUT
policy=ACCEPT
logging=enabled
log_file=/var/log/personal-firewall.log

[rules]
# Add your custom rules below
# Format: ID. action=ACTION, source=IP, port=PORT, protocol=PROTOCOL, comment="TEXT"

# Example rule - uncomment to use:
# 1. action=ACCEPT, protocol=TCP, port=22, comment="Allow SSH"

# Start your rules here:

